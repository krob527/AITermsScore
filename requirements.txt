# Web interface
Flask==3.1.3
gunicorn>=22.0.0          # Production WSGI server (Linux App Service – not installed on Windows dev)

# Web search (DuckDuckGo – no API key required)
ddgs==9.10.0

# Azure AI – Agents SDK & project client
azure-ai-projects==1.0.0
azure-ai-agents==1.1.0

# Azure identity (DefaultAzureCredential – works with az login, Managed Identity, etc.)
azure-identity==1.25.2

# HTTP utilities (for any supplemental URL fetching)
httpx==0.28.1

# Output rendering
Jinja2==3.1.6          # HTML scorecard template rendering
Markdown==3.10.2       # Optional: convert Markdown to HTML standalone

# CLI / UX
rich==14.3.3           # Pretty console output
click==8.3.1           # CLI argument parsing

# Environment loading
python-dotenv==1.2.1
